import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.voyageclient',
  appName: 'voyageclient',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
